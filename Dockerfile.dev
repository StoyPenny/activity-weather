# Development Dockerfile – Vite dev server with hot module replacement
FROM node:20-alpine

WORKDIR /app

# Install all dependencies (including devDependencies)
COPY package*.json ./
RUN npm ci

EXPOSE 4175

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
